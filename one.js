//git init - инициализирован пустой репозиторий
//create '.gitignore' and add to it /.idea
// git add - file(s) and '.gitignore - green'
// first commit


// А тут напишем Function Declaration, такую же по смыслу, как и в файле two.js

function f(n) {
		return n ** 2;
}

function sum(x, y) {
		  return x + y;
}

// создал функцию для сложения двух чисел


function dif(x, y) {
		return x - y;
}

// у меня новый комментарий

// создаем кнопки